/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
 */

import { motion } from "motion/react";
import { 
  Phone, 
  Home, 
  Grid, 
  Bell, 
  User, 
  Droplets, 
  Search, 
  Wrench, 
  Camera, 
  Wind, 
  Waves, 
  ThermometerSnowflake, 
  ShieldCheck,
  Trophy,
  MapPin,
  Car,
  CircleDollarSign,
  Shield,
  Star,
  ChevronDown,
  ArrowLeftRight
} from "lucide-react";

const CATEGORIES = [
  { icon: <Droplets className="w-7 h-7" />, name: "누수탐지" },
  { icon: <Search className="w-7 h-7" />, name: "점검" },
  { icon: <Waves className="w-7 h-7" />, name: "배관막힘" },
  { icon: <Wrench className="w-7 h-7" />, name: "각종부품교체" },
  { icon: <Wrench className="w-7 h-7" />, name: "배관공사 & 교체" },
  { icon: <Camera className="w-7 h-7" />, name: "내시경검수" },
  { icon: <Wind className="w-7 h-7" />, name: "악취제거" },
  { icon: <Waves className="w-7 h-7" />, name: "준설공사" },
  { icon: <ThermometerSnowflake className="w-7 h-7" />, name: "해빙" },
  { icon: <ShieldCheck className="w-7 h-7" />, name: "방수공사" },
];

const WHY_JINGO = [
  { 
    icon: <Trophy className="text-gold w-8 h-8" />, 
    title: "소비자 만족도 1위", 
    desc: "높은 완성도로 검증된 결과. 일반 업체와 다른 차원의 퀄리티를 경험하세요.",
    wide: true 
  },
  { 
    icon: <MapPin className="text-gold w-7 h-7" />, 
    title: "100% 현장 방문 상담", 
    desc: "전화가 아닌 직접 방문 후 정확한 진단 & 시공 진행" 
  },
  { 
    icon: <Car className="text-gold w-7 h-7" />, 
    title: "출장비 무료", 
    desc: "현장 견적 후 협의. 숨겨진 비용 없이 투명하게" 
  },
  { 
    icon: <CircleDollarSign className="text-gold w-7 h-7" />, 
    title: "합리적인 비용", 
    desc: "높은 완성도 대비 합리적인 가격 보장" 
  },
  { 
    icon: <Shield className="text-gold w-7 h-7" />, 
    title: "A/S 1년 무상", 
    desc: "시공 후 1년간 무상 A/S로 마음 편하게" 
  },
];

const GALLERY_ITEMS = [
  { img: "https://images.unsplash.com/photo-1504328345606-18bbc8c9d7d1?w=600&q=80", badge: "누수탐지", label: "지하 배관 누수 탐지 완료", tall: true },
  { img: "https://images.unsplash.com/photo-1558618666-fcd25c85cd64?w=400&q=80", badge: "배관공사", label: "노후 배관 전면 교체" },
  { img: "https://images.unsplash.com/photo-1621905251189-08b45d6a269e?w=400&q=80", badge: "방수공사", label: "옥상 방수 시공" },
  { img: "https://images.unsplash.com/photo-1581578731548-c64695cc6952?w=400&q=80", badge: "내시경검수", label: "내시경 정밀 검수" },
  { img: "https://images.unsplash.com/photo-1585771724684-38269d6639fd?w=400&q=80", badge: "준설공사", label: "하수구 준설 작업" },
];

const BEFORE_AFTER = [
  {
    before: "https://images.unsplash.com/photo-1542013936693-884638332954?w=400&q=80",
    after: "https://images.unsplash.com/photo-1584622650111-993a426fbf0a?w=400&q=80",
    title: "욕실 배관 교체 시공",
    meta: "경기 용인 · 2024.11"
  },
  {
    before: "https://images.unsplash.com/photo-1598300042247-d088f8ab3a91?w=400&q=80",
    after: "https://images.unsplash.com/photo-1556909114-f6e7ad7d3136?w=400&q=80",
    title: "주방 하수구 막힘 해결",
    meta: "경기 수원 · 2024.12"
  },
  {
    before: "https://images.unsplash.com/photo-1571781565036-d3f759be73e4?w=400&q=80",
    after: "https://images.unsplash.com/photo-1600607687644-c7171b42498b?w=400&q=80",
    title: "옥상 방수 전면 시공",
    meta: "서울 강남 · 2025.01"
  }
];

const TEAM = [
  { name: "김○○ 마스터", role: "누수탐지 전문", career: "경력 18년\n누수탐지 기사 1급", rating: "4.9", img: "https://images.unsplash.com/photo-1560250097-0b93528c311a?w=300&q=80" },
  { name: "박○○ 마스터", role: "배관공사 전문", career: "경력 22년\n배관기능장 자격", rating: "5.0", img: "https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=300&q=80" },
  { name: "이○○ 마스터", role: "방수공사 전문", career: "경력 15년\n방수기능사 자격", rating: "4.8", img: "https://images.unsplash.com/photo-1472099645785-5658abf4ff4e?w=300&q=80" },
  { name: "최○○ 마스터", role: "악취·준설 전문", career: "경력 12년\n환경처리 전문가", rating: "4.9", img: "https://images.unsplash.com/photo-1500648767791-00dcc994a43e?w=300&q=80" },
];

const REVIEWS = [
  { img: "https://images.unsplash.com/photo-1584622650111-993a426fbf0a?w=500&q=80", text: "다른 업체 3곳에서 못 찾은 누수를 바로 찾아냈어요. 깔끔하게 해결해주셔서 너무 감사합니다.", author: "김*진 고객님 · 용인", date: "2025년 1월", initial: "김" },
  { img: "https://images.unsplash.com/photo-1556909114-f6e7ad7d3136?w=500&q=80", text: "출장비도 없고 현장 견적 후 바로 진행. 가격도 합리적이고 결과물이 정말 깔끔했습니다!", author: "박*현 고객님 · 수원", date: "2024년 12월", initial: "박" },
  { img: "https://images.unsplash.com/photo-1600607687644-c7171b42498b?w=500&q=80", text: "악취가 몇 달째 해결 안 됐는데 진고 마스터님이 근본 원인까지 완벽히 잡아주셨어요. A/S도 믿을 수 있어요.", author: "이*영 고객님 · 성남", date: "2024년 11월", initial: "이" },
  { img: "https://images.unsplash.com/photo-1558618666-fcd25c85cd64?w=500&q=80", text: "방수 공사 1년 지났는데 완벽합니다. 1년 무상 A/S 덕분에 걱정 없이 지내고 있어요. 강력 추천!", author: "최*호 고객님 · 강남", date: "2024년 10월", initial: "최" },
];

export default function App() {
  const scrollToSection = (id: string) => {
    const element = document.getElementById(id);
    if (element) {
      element.scrollIntoView({ behavior: "smooth" });
    }
  };

  return (
    <div className="min-h-screen pb-20">
      {/* HEADER */}
      <header className="fixed top-0 left-0 right-0 z-50 bg-navy/92 backdrop-blur-xl border-b border-gold/20 h-[60px] flex items-center justify-between px-5">
        <div className="flex items-center gap-2">
          <div className="w-9 h-9 bg-linear-to-br from-gold to-gold-light rounded-lg flex items-center justify-center font-display text-navy text-base font-black tracking-tighter">
            진고
          </div>
          <span className="font-display text-xl tracking-widest bg-linear-to-r from-gold to-gold-light bg-clip-text text-transparent">
            JINGO
          </span>
        </div>
        <a href="tel:01092795589" className="flex items-center gap-1.5 text-[13px] text-gold font-bold no-underline">
          <Phone className="w-4 h-4 fill-gold" />
          010-9279-5589
        </a>
      </header>

      <main className="pt-[60px]">
        {/* HERO */}
        <section id="home" className="relative min-h-[100svh] flex flex-col justify-center items-start px-6 py-10 overflow-hidden">
          <div className="absolute inset-0 z-0 bg-navy">
            <div className="absolute inset-0 bg-[radial-gradient(ellipse_80%_60%_at_80%_30%,rgba(58,156,247,0.12)_0%,transparent_60%)]" />
            <div className="absolute inset-0 bg-[radial-gradient(ellipse_60%_50%_at_20%_70%,rgba(232,160,32,0.1)_0%,transparent_60%)]" />
          </div>

          <motion.div 
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            className="relative z-10 inline-flex items-center gap-1.5 bg-gold/15 border border-gold/40 rounded-full px-3.5 py-1.5 text-xs text-gold font-medium mb-5"
          >
            <div className="w-1.5 h-1.5 rounded-full bg-gold animate-pulse-custom" />
            24시간 무료상담 · 무료출장
          </motion.div>

          <motion.h1 
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.1 }}
            className="relative z-10 font-display text-[clamp(28px,8vw,42px)] leading-tight mb-4"
          >
            실력으로 증명하고,<br/>
            <span className="bg-linear-to-r from-gold to-gold-light bg-clip-text text-transparent">가격으로 놀라게 합니다.</span>
          </motion.h1>

          <motion.p 
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.2 }}
            className="relative z-10 text-sm text-[#a0aec0] leading-relaxed mb-8"
          >
            진짜고수는 해당 분야에서 검증된<br/>
            최고의 닥터들이 방문드립니다.<br/>
            일반업체와 결과물의 차이를 경험하세요.
          </motion.p>

          <motion.div 
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.3 }}
            className="relative z-10 flex gap-3 flex-wrap"
          >
            <a href="tel:01092795589" className="inline-flex items-center gap-2 bg-linear-to-br from-gold to-gold-light text-navy font-bold text-sm px-6 py-3.5 rounded-xl shadow-[0_4px_20px_rgba(232,160,32,0.4)] active:scale-95 transition-transform">
              <Phone className="w-4 h-4" strokeWidth={2.5} />
              지금 바로 상담
            </a>
            <button 
              onClick={() => scrollToSection('categories')}
              className="inline-flex items-center gap-2 bg-transparent border-1.5 border-gold/50 text-gold font-semibold text-sm px-6 py-3.5 rounded-xl active:bg-gold/15 transition-colors"
            >
              서비스 보기
            </button>
          </motion.div>

          <motion.div 
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.4 }}
            className="relative z-10 flex gap-6 mt-10"
          >
            <div className="text-center">
              <div className="font-display text-[28px] bg-linear-to-r from-gold to-gold-light bg-clip-text text-transparent">1위</div>
              <div className="text-[11px] text-gray-400 mt-0.5">고객 만족도</div>
            </div>
            <div className="text-center">
              <div className="font-display text-[28px] bg-linear-to-r from-gold to-gold-light bg-clip-text text-transparent">1년</div>
              <div className="text-[11px] text-gray-400 mt-0.5">무상 A/S</div>
            </div>
            <div className="text-center">
              <div className="font-display text-[28px] bg-linear-to-r from-gold to-gold-light bg-clip-text text-transparent">무료</div>
              <div className="text-[11px] text-gray-400 mt-0.5">현장 출장</div>
            </div>
          </motion.div>

          <div className="absolute bottom-7 left-1/2 -translate-x-1/2 z-10 flex flex-col items-center gap-1.5 animate-bounce-custom">
            <span className="text-[10px] text-gray-400 tracking-[2px]">SCROLL</span>
            <div className="w-px h-10 bg-linear-to-b from-gold to-transparent" />
          </div>
        </section>

        {/* WHY JINGO */}
        <section id="why" className="bg-navy-light px-5 py-15">
          <p className="text-[11px] tracking-[3px] text-gold font-semibold uppercase mb-2.5">Why JINGO</p>
          <h2 className="font-display text-[clamp(22px,6vw,30px)] leading-tight mb-2">진고를 선택해야<br/>하는 이유</h2>
          <p className="text-sm text-gray-400 leading-relaxed mb-8">최선의 노력으로도 해결 안 됐다면,<br/>이제 진짜 고수를 불러주세요.</p>
          
          <div className="grid grid-cols-2 gap-3">
            {WHY_JINGO.map((item, idx) => (
              <motion.div 
                key={idx}
                whileInView={{ opacity: 1, y: 0 }}
                initial={{ opacity: 0, y: 20 }}
                viewport={{ once: true }}
                className={`bg-navy-lighter border border-gold/15 rounded-xl p-5 relative overflow-hidden active:scale-[0.98] transition-transform ${item.wide ? 'col-span-2' : ''}`}
              >
                <div className="absolute top-0 left-0 right-0 h-0.5 bg-linear-to-r from-gold to-transparent opacity-0 group-active:opacity-100 transition-opacity" />
                <div className="mb-2.5">{item.icon}</div>
                <div className="text-[13px] font-bold mb-1.5 text-white">{item.title}</div>
                <div className="text-xs text-gray-400 leading-relaxed">{item.desc}</div>
              </motion.div>
            ))}
          </div>
        </section>

        {/* CATEGORIES */}
        <section id="categories" className="bg-navy px-5 py-15">
          <p className="text-[11px] tracking-[3px] text-gold font-semibold uppercase mb-2.5">Categories</p>
          <h2 className="font-display text-[clamp(22px,6vw,30px)] leading-tight mb-2">서비스 카테고리</h2>
          <p className="text-sm text-gray-400 leading-relaxed mb-8">생활 속 모든 문제, 진고 전문가가 해결합니다.</p>
          
          <div className="grid grid-cols-3 gap-2.5">
            {CATEGORIES.map((cat, idx) => (
              <motion.div 
                key={idx}
                whileInView={{ opacity: 1, scale: 1 }}
                initial={{ opacity: 0, scale: 0.9 }}
                viewport={{ once: true }}
                className="bg-navy-lighter border border-white/5 rounded-2xl p-4.5 flex flex-col items-center gap-2 cursor-pointer active:scale-95 active:bg-gold/15 active:border-gold transition-all text-center"
              >
                <div className="text-gold">{cat.icon}</div>
                <div className="text-[11px] font-semibold text-[#c8d2e0]">{cat.name}</div>
              </motion.div>
            ))}
          </div>
        </section>

        {/* ABOUT */}
        <section id="about" className="bg-linear-to-br from-navy-light to-navy px-5 py-15">
          <p className="text-[11px] tracking-[3px] text-gold font-semibold uppercase mb-2.5">About JINGO</p>
          <h2 className="font-display text-[clamp(22px,6vw,30px)] leading-tight mb-8">편리미엄 생활전문서비스<br/>진고를 만나다</h2>
          
          <div className="bg-navy-lighter border border-gold/20 rounded-3xl p-7 relative overflow-hidden">
            <div className="absolute -right-2.5 -bottom-5 font-display text-[100px] text-gold/5 pointer-events-none">진고</div>
            <p className="font-display text-lg leading-snug text-gold-light mb-5">"한 분야의 최고 경지에 도달한 사람, 그것이 진짜고수입니다."</p>
            <p className="text-sm text-gray-400 leading-loose">
              <strong className="text-[#c8d2e0]">진고</strong>는 <strong className="text-[#c8d2e0]">진짜고수</strong>의 줄임말입니다.<br/><br/>
              지나치게 많은 광고나 저가 경쟁보다는 근본적인 원인 해결로 고객님의 소중한 시간과 노력을 아껴드립니다.<br/><br/>
              많은 시행착오를 거쳐 최고의 경지에 도달한 전문가들이 직접 방문합니다.
            </p>
          </div>
        </section>

        {/* GALLERY */}
        <section id="gallery" className="bg-navy-light px-5 py-15">
          <p className="text-[11px] tracking-[3px] text-gold font-semibold uppercase mb-2.5">실제 시공 사례</p>
          <h2 className="font-display text-[clamp(22px,6vw,30px)] leading-tight mb-2">직접 확인하세요,<br/>진고의 현장</h2>
          <p className="text-sm text-gray-400 leading-relaxed mb-5">과장 없이 실제 시공 현장 사진을 공개합니다.</p>
          
          <div className="flex gap-2 mb-5 overflow-x-auto pb-2 scrollbar-hide">
            {["전체", "누수탐지", "배관막힘", "방수공사", "악취제거"].map((tab, idx) => (
              <button key={idx} className={`flex-shrink-0 text-xs font-semibold px-4 py-2 rounded-full border transition-all ${idx === 0 ? 'bg-gold/15 border-gold text-gold' : 'bg-navy-lighter border-white/10 text-gray-400'}`}>
                {tab}
              </button>
            ))}
          </div>

          <div className="grid grid-cols-2 gap-2.5">
            {GALLERY_ITEMS.map((item, idx) => (
              <motion.div 
                key={idx}
                whileInView={{ opacity: 1, scale: 1 }}
                initial={{ opacity: 0, scale: 0.95 }}
                viewport={{ once: true }}
                className={`relative rounded-2xl overflow-hidden bg-navy-lighter cursor-pointer group ${item.tall ? 'row-span-2 aspect-auto' : 'aspect-4/3'}`}
              >
                <img src={item.img} alt={item.label} className="w-full h-full object-cover transition-transform duration-500 group-active:scale-105" />
                <span className="absolute top-2.5 left-2.5 bg-gold text-navy text-[10px] font-extrabold px-2 py-0.5 rounded-md">{item.badge}</span>
                <div className="absolute inset-0 bg-linear-to-t from-black/80 via-transparent to-transparent flex items-end p-3">
                  <div className="text-white text-[11px] font-semibold">{item.label}</div>
                </div>
              </motion.div>
            ))}
          </div>
          
          <div className="mt-4 text-center">
            <button className="inline-flex items-center gap-1.5 border-1.5 border-gold/40 text-gold text-[13px] font-semibold px-6 py-3 rounded-xl active:bg-gold/10 transition-colors">
              더 많은 사례 보기
              <ChevronDown className="w-3.5 h-3.5" />
            </button>
          </div>
        </section>

        {/* BEFORE & AFTER */}
        <section id="before-after" className="bg-navy px-5 py-15">
          <p className="text-[11px] tracking-[3px] text-gold font-semibold uppercase mb-2.5">Before & After</p>
          <h2 className="font-display text-[clamp(22px,6vw,30px)] leading-tight mb-2">시공 전·후<br/>결과를 보여드립니다</h2>
          <p className="text-sm text-gray-400 leading-relaxed mb-8">진고의 완성도, 사진으로 직접 확인하세요.</p>

          {BEFORE_AFTER.map((item, idx) => (
            <motion.div 
              key={idx}
              whileInView={{ opacity: 1, y: 0 }}
              initial={{ opacity: 0, y: 20 }}
              viewport={{ once: true }}
              className="mb-4 rounded-2xl overflow-hidden bg-navy-lighter border border-white/10"
            >
              <div className="grid grid-cols-2 relative">
                <div className="absolute top-0 bottom-0 left-1/2 w-0.5 bg-gold z-10 -translate-x-1/2" />
                <div className="relative aspect-square overflow-hidden">
                  <img src={item.before} alt="before" className="w-full h-full object-cover" />
                  <span className="absolute bottom-2 left-2 bg-black/70 text-[#ff6b6b] text-[10px] font-extrabold px-2 py-1 rounded-md">BEFORE</span>
                </div>
                <div className="relative aspect-square overflow-hidden">
                  <img src={item.after} alt="after" className="w-full h-full object-cover" />
                  <span className="absolute bottom-2 right-2 bg-gold/90 text-navy text-[10px] font-extrabold px-2 py-1 rounded-md">AFTER</span>
                </div>
                <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-7 h-7 rounded-full bg-gold z-20 flex items-center justify-center text-navy">
                  <ArrowLeftRight className="w-3.5 h-3.5" strokeWidth={3} />
                </div>
              </div>
              <div className="p-4 flex items-center justify-between">
                <div>
                  <div className="text-[13px] font-bold text-white">{item.title}</div>
                  <div className="text-[11px] text-gray-400 mt-0.5">{item.meta}</div>
                </div>
                <div className="text-[11px] font-bold text-gold bg-gold/15 px-2.5 py-1 rounded-lg">✅ 완벽 해결</div>
              </div>
            </motion.div>
          ))}
        </section>

        {/* TEAM */}
        <section id="team" className="bg-navy-light px-5 py-15">
          <p className="text-[11px] tracking-[3px] text-gold font-semibold uppercase mb-2.5">Our Experts</p>
          <h2 className="font-display text-[clamp(22px,6vw,30px)] leading-tight mb-2">검증된 전문가들이<br/>직접 방문합니다</h2>
          <p className="text-sm text-gray-400 leading-relaxed mb-8">화려한 광고 대신, 실력으로 증명하는 진짜 고수들</p>
          
          <div className="flex gap-3.5 overflow-x-auto pb-4 -mx-5 px-5 scrollbar-hide">
            {TEAM.map((member, idx) => (
              <motion.div 
                key={idx}
                whileInView={{ opacity: 1, scale: 1 }}
                initial={{ opacity: 0, scale: 0.95 }}
                viewport={{ once: true }}
                className="flex-shrink-0 w-40 bg-navy-lighter rounded-2xl overflow-hidden border border-white/10"
              >
                <img src={member.img} alt={member.name} className="w-full aspect-3/4 object-cover object-top" />
                <div className="p-3">
                  <div className="text-sm font-bold mb-1 text-white">{member.name}</div>
                  <div className="text-[11px] text-gold font-semibold mb-1.5">{member.role}</div>
                  <div className="text-[11px] text-gray-400 leading-relaxed whitespace-pre-line">{member.career}</div>
                  <div className="text-xs text-gold mt-1.5 flex items-center gap-1">
                    <Star className="w-3 h-3 fill-gold" />
                    {member.rating}
                  </div>
                </div>
              </motion.div>
            ))}
          </div>
        </section>

        {/* REVIEWS */}
        <section id="reviews" className="bg-navy px-5 py-15">
          <p className="text-[11px] tracking-[3px] text-gold font-semibold uppercase mb-2.5">Real Reviews</p>
          <h2 className="font-display text-[clamp(22px,6vw,30px)] leading-tight mb-2">실제 고객 후기</h2>
          <p className="text-sm text-gray-400 leading-relaxed mb-8">과장 없는 진짜 리뷰, 직접 읽어보세요.</p>
          
          <div className="flex gap-3.5 overflow-x-auto -mx-5 px-5 scrollbar-hide">
            {REVIEWS.map((review, idx) => (
              <motion.div 
                key={idx}
                whileInView={{ opacity: 1, x: 0 }}
                initial={{ opacity: 0, x: 20 }}
                viewport={{ once: true }}
                className="flex-shrink-0 w-[280px] bg-navy-lighter border border-white/10 rounded-2xl p-4.5"
              >
                <img src={review.img} alt="시공 후" className="w-full aspect-4/3 rounded-xl object-cover mb-3" />
                <div className="flex gap-0.5 text-gold mb-2">
                  {[...Array(5)].map((_, i) => <Star key={i} className="w-3.5 h-3.5 fill-gold" />)}
                </div>
                <p className="text-[13px] text-gray-400 leading-relaxed mb-3">{review.text}</p>
                <div className="flex items-center gap-2">
                  <div className="w-8 h-8 rounded-full bg-gold/15 border border-gold flex items-center justify-center text-[13px] font-bold text-gold">
                    {review.initial}
                  </div>
                  <div>
                    <div className="text-xs font-bold text-white">{review.author}</div>
                    <div className="text-[10px] text-gray-500">{review.date}</div>
                  </div>
                </div>
              </motion.div>
            ))}
          </div>
        </section>

        {/* CTA BANNER */}
        <div className="mx-5 mb-15 bg-linear-to-br from-gold to-[#c97a00] rounded-3xl p-8 text-center shadow-[0_12px_40px_rgba(232,160,32,0.3)]">
          <h3 className="font-display text-[22px] text-navy mb-2">지금 바로 상담하세요</h3>
          <p className="text-[13px] text-navy/75 mb-5">24시간 언제든지 무료 출장 · 무료 상담<br/>출장비 없이 현장 견적 후 진행합니다</p>
          <a href="tel:01092795589" className="inline-flex items-center gap-2 bg-navy text-gold font-display text-xl px-7 py-3.5 rounded-2xl no-underline shadow-[0_4px_16px_rgba(0,0,0,0.3)]">
            <Phone className="w-5 h-5" strokeWidth={2.5} />
            010-9279-5589
          </a>
        </div>

        {/* FOOTER */}
        <footer className="bg-[#070d1a] border-t border-gold/15 px-6 py-9 pb-5">
          <div className="font-display text-[22px] tracking-widest bg-linear-to-r from-gold to-gold-light bg-clip-text text-transparent mb-5">JINGO 진고</div>
          <div className="flex gap-5 flex-wrap mb-6">
            <a href="#" className="text-[13px] text-gray-400 no-underline">홈</a>
            <a href="#" className="text-[13px] text-gray-400 no-underline">카테고리</a>
            <a href="#" className="text-[13px] text-gray-400 no-underline">새소식</a>
            <a href="#" className="text-[13px] text-gray-400 no-underline">마이페이지</a>
          </div>
          <div className="text-xs text-[#4a5568] leading-loose">
            <strong className="text-gray-400">상호:</strong> JINGO 진고<br/>
            <strong className="text-gray-400">주소:</strong> 경기도 용인시 기흥구 죽전로10 6층 601호<br/>
            <strong className="text-gray-400">전화:</strong> 010-9279-5589<br/>
            <strong className="text-gray-400">팩스:</strong> 031-229-2110<br/>
            <strong className="text-gray-400">메일:</strong> titanstool628@gmail.com
          </div>
          <p className="text-[11px] text-[#2d3748] mt-6">© 2025 JINGO 진고. All rights reserved.</p>
        </footer>
      </main>

      {/* BOTTOM NAV */}
      <nav className="fixed bottom-0 left-0 right-0 z-50 bg-navy/97 border-t border-gold/20 flex justify-around items-center h-16 pb-[env(safe-area-inset-bottom)]">
        <div className="flex flex-col items-center gap-1 text-[10px] text-gold cursor-pointer px-4 py-2" onClick={() => window.scrollTo({ top: 0, behavior: 'smooth' })}>
          <Home className="w-[22px] h-[22px]" />
          홈
        </div>
        <div className="flex flex-col items-center gap-1 text-[10px] text-gray-400 cursor-pointer px-4 py-2" onClick={() => scrollToSection('categories')}>
          <Grid className="w-[22px] h-[22px]" />
          카테고리
        </div>
        <div className="flex flex-col items-center gap-1 text-[10px] text-gray-400 cursor-pointer px-4 py-2">
          <Bell className="w-[22px] h-[22px]" />
          새소식
        </div>
        <div className="flex flex-col items-center gap-1 text-[10px] text-gray-400 cursor-pointer px-4 py-2">
          <User className="w-[22px] h-[22px]" />
          마이페이지
        </div>
        <a href="tel:01092795589" className="flex flex-col items-center gap-1 text-[10px] text-gray-400 cursor-pointer px-4 py-2 no-underline">
          <Phone className="w-[22px] h-[22px]" />
          상담전화
        </a>
      </nav>
    </div>
  );
}
